{
  "name": "webframe",
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "rm -rf ./.nx/cache/",
          "rm -rf ./.nx/installation/",
          "rm -rf ./.nx/workspace-data/",
          "rm -rf ./artifact/",
          "rm -rf ./coverage/",
          "rm -rf ./tmp/"
        ],
        "parallel": true
      }
    }
  }
}
